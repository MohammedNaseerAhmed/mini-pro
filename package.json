{
  "name": "mini-pro",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "powershell -ExecutionPolicy Bypass -File scripts/setup.ps1",
    "frontend:install": "npm install --prefix frontend",
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "backend:install": "powershell -ExecutionPolicy Bypass -File scripts/install-backend.ps1",
    "backend:dev": "powershell -ExecutionPolicy Bypass -File scripts/run-backend.ps1"
  }
}
